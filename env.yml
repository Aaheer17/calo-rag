name: calo-rag
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - langchain>=0.3.0
      - langchain-community>=0.3.0
      - sentence-transformers>=3.0.0
      - faiss-cpu
      - pypdf>=4.0.0
      - numpy
      - python-dotenv
      - tqdm
      - rich
